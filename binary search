#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int arr[] = {1, 3, 5, 7, 9, 11};
    int n = sizeof(arr) / sizeof(arr[0]);

    if (binary_search(arr, arr + n, 7))
        cout << "Found";
    else
        cout << "Not Found";

    return 0;
}
